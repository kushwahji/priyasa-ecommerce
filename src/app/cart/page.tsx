'use client';
import Link from 'next/link';import {useEffect,useState} from'react';import{money}from'@/lib/catalog';import{CheckIcon,CloseIcon,SearchIcon,GiftIcon,TruckIcon}from'@/components/StorefrontIcons';
type Item={variantId:string;productId:string;name:string;price:number;quantity:number;image?:string};
export default function Cart(){const[items,setItems]=useState<Item[]>([]);const[coupon,setCoupon]=useState('');const[quote,setQuote]=useState<any>(null);const[msg,setMsg]=useState('');const[msgType,setMsgType]=useState<'success'|'error'>('success');
async function refreshQuote(list:Item[],code=''){if(!list.length){setQuote(null);return}const r=await fetch('/api/checkout/quote',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:list.map(i=>({variantId:i.variantId,quantity:i.quantity})),coupon:code||undefined})});const d=await r.json().catch(()=>({}));if(!r.ok){setMsg(d.error||'Unable to validate your bag.');setMsgType('error');setQuote(null);return}setQuote(d);if(d.coupon)setCoupon(d.coupon)}
async function sync(next:Item[],code=coupon,notice?:string){setItems(next);localStorage.setItem('priyasa_cart',JSON.stringify(next));window.dispatchEvent(new Event('priyasa-cart-updated'));if(notice){setMsg(notice);setMsgType('success')}const r=await fetch('/api/cart',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:next.map(i=>({variantId:i.variantId,quantity:i.quantity})),couponCode:code||undefined})});if(!r.ok){const d=await r.json().catch(()=>({}));setMsg(d.error||'Some cart items are unavailable.');setMsgType('error');return}await refreshQuote(next,code)}
useEffect(()=>{let alive=true;(async()=>{try{const local:Item[]=JSON.parse(localStorage.getItem('priyasa_cart')||'[]');const r=await fetch('/api/cart',{cache:'no-store'});const d=await r.json();const server:Item[]=d.data?.items||[];const merged=[...server];for(const i of local){const found=merged.find(x=>x.variantId===i.variantId);if(found)found.quantity=Math.min(20,found.quantity+i.quantity);else merged.push(i)}if(merged.length!==server.length||local.length)await sync(merged,d.data?.couponCode||'');else if(alive){setItems(server);setCoupon(d.data?.couponCode||'');await refreshQuote(server,d.data?.couponCode||'')}}catch{const local=JSON.parse(localStorage.getItem('priyasa_cart')||'[]');if(alive){setItems(local);await refreshQuote(local)}}})();const load=()=>{try{const local=JSON.parse(localStorage.getItem('priyasa_cart')||'[]');setItems(local);void refreshQuote(local,coupon)}catch{}};window.addEventListener('priyasa-cart-updated',load);return()=>{alive=false;window.removeEventListener('priyasa-cart-updated',load)}},[]);
useEffect(()=>{if(!msg)return;const t=window.setTimeout(()=>setMsg(''),2600);return()=>window.clearTimeout(t)},[msg]);
const subtotal=quote?.subtotal??items.reduce((s,i)=>s+i.price*i.quantity,0);const discount=quote?.discount??0;const shipping=quote?.shipping??(subtotal>999?0:99);const total=quote?.total??subtotal+shipping-discount;const saved=Math.max(0,subtotal+shipping-total);const remaining=Math.max(0,999-(subtotal-discount));
const change=(next:Item[],notice?:string)=>void sync(next,coupon,notice);const apply=(code=coupon.trim().toUpperCase())=>{setCoupon(code);void sync(items,code,`Checking offer ${code}…`)};
return <div className="storefront-page nykaa-cart-v1"><div className="nykaa-mobile-head"><Link href="/" className="back" aria-label="Back">‹</Link><h1>Bag <small>{items.reduce((s,i)=>s+i.quantity,0)} {items.reduce((s,i)=>s+i.quantity,0)===1?'item':'items'}</small></h1><Link href="/search" className="head-action" aria-label="Search"><SearchIcon/></Link></div><div className="page storefront-inner cart-page"><div className="breadcrumbs"><Link href="/">Home</Link> / Cart</div><div className="cart-heading"><div><span className="eyebrow dark">YOUR EDIT</span><h1>Shopping bag</h1></div><span>{items.reduce((s,i)=>s+i.quantity,0)} items</span></div>{msg&&<div className={`cart-notice ${msgType}`} role="status" aria-live="polite"><CheckIcon/><span>{msg}</span><button aria-label="Dismiss" onClick={()=>setMsg('')}><CloseIcon/></button></div>}{items.length===0?<div className="empty-cart"><h2>Your bag is waiting</h2><p>Discover something beautiful and add it to your Priyasa edit.</p><Link className="button dark-button" href="/shop">Continue shopping</Link></div>:<><div className="cart-layout"><section className="cart-items"><div className="nykaa-delivery-head"><span>Items · {items.reduce((s,i)=>s+i.quantity,0)}</span><strong><TruckIcon/> Standard Delivery</strong></div>{items.map((i,idx)=><article className="cart-product" key={i.variantId}><div className="cart-product-image">{i.image?<img src={i.image} alt={i.name}/>:<div/>}</div><div className="cart-product-info"><div><h3>{i.name}</h3><p>{money(i.price)}</p></div><button className="cart-remove" onClick={()=>change(items.filter((_,j)=>j!==idx),`${i.name} removed from your bag.`)} aria-label={`Remove ${i.name} from cart`}>Remove</button><div className="cart-product-bottom"><div className="qty-control"><button type="button" aria-label={`Decrease quantity of ${i.name}`} disabled={i.quantity<=1} onClick={()=>{const n=items.map((x,j)=>j===idx?{...x,quantity:x.quantity-1}:x);change(n,`${i.name} quantity decreased.`)}}>−</button><span aria-live="polite">{i.quantity}</span><button type="button" aria-label={`Increase quantity of ${i.name}`} disabled={i.quantity>=20} onClick={()=>{const n=items.map((x,j)=>j===idx?{...x,quantity:x.quantity+1}:x);change(n,`${i.name} quantity increased.`)}}>+</button></div><strong>{money((quote?.items?.find((x:any)=>x.variantId===i.variantId)?.unitPrice??i.price)*i.quantity)}</strong></div></div></article>)}<div className="nykaa-cart-payline"><span>You Pay <small>ⓘ</small></span><div><strong>{money(total)}</strong>{quote?.subtotal&&discount>0&&<><del>{money(subtotal+shipping)}</del><b>{Math.round((discount/Math.max(1,subtotal))*100)}% off</b></>}</div></div><Link className="continue-shopping" href="/shop">← Continue shopping</Link></section><aside className="cart-summary"><h2>Order summary</h2><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div><span>Discount</span><strong className="green">-{money(discount)}</strong></div><div><span>Shipping</span><strong>{shipping?'₹99':'Free'}</strong></div><hr/><div className="cart-total"><span>Total</span><strong>{money(total)}</strong></div><label className="coupon-box"><input value={coupon} maxLength={40} onChange={e=>setCoupon(e.target.value.toUpperCase())} placeholder="Coupon code"/><button type="button" onClick={()=>apply()}>Apply</button></label>{quote?.coupon&&<small className="green">{quote.coupon} applied</small>}<Link className="button dark-button cart-checkout" href="/checkout">Proceed</Link><p className="summary-note">Live price & stock validation · Secure checkout · Easy returns</p></aside></div><section className="nykaa-offers-v1"><h2>Coupons and Offers</h2><div className="nykaa-offer-card"><div className="nykaa-offer-main"><span className="nykaa-offer-icon"><GiftIcon/></span><div><strong>Save up to ₹89</strong><span>Extra 15% Off upto ₹300</span></div></div><div className="nykaa-offer-bottom"><span>NEW15</span><button type="button" onClick={()=>apply('NEW15')}>{coupon==='NEW15'?'Applied':'Apply'}</button></div></div><div className="nykaa-offer-all"><Link href="/offers">View All ›</Link></div></section><section className="nykaa-price-card"><div className="nykaa-price-row"><span>Price details</span><strong>{money(total)}⌄</strong></div>{saved>0&&<div className="nykaa-save">You are saving <strong>{money(saved)}</strong> on this order</div>}</section></>}</div></div>}
