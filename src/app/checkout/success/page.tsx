import Link from 'next/link';
export default async function CheckoutSuccess({searchParams}:{searchParams:Promise<{order?:string}>}){const p=await searchParams;return <div className="page"><div className="form" style={{maxWidth:680,margin:'60px auto',textAlign:'center'}}><h1 style={{font:'normal 42px Georgia,serif'}}>Thank you for your order.</h1><p className="muted">{p.order?`Order ${p.order} has been received.`:'Your order has been received.'}</p><p>Payment verification is complete. You will receive order updates through your available notification channels.</p><div style={{display:'flex',gap:12,justifyContent:'center',marginTop:24}}><Link className="button" href="/track-order">Track Order</Link><Link className="button secondary" href="/shop">Continue Shopping</Link></div></div></div>}
