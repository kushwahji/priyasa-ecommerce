'use client';
import { useState } from 'react';
import Link from 'next/link';
export default function Checkout(){const [form,setForm]=useState({fullName:'',phone:'',line1:'',city:'',state:'Madhya Pradesh',pincode:'',coupon:''});const [status,setStatus]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setStatus('Creating secure order…');const items=JSON.parse(localStorage.getItem('priyasa_cart')||'[]').map((x:any)=>({variantId:x.variantId,quantity:x.quantity}));if(!items.length){setStatus('Your cart is empty.');return}const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,items})});const data=await r.json();if(!r.ok){setStatus(data.error||'Unable to create order');return}localStorage.removeItem('priyasa_cart');setStatus(`Order ${data.orderNumber} created. Payment setup is ready on the server.`);}return <div className="page"><div className="breadcrumbs">Home / Checkout</div><div className="shop-layout" style={{gridTemplateColumns:'1fr 360px'}}><form className="form" onSubmit={submit}><h1 style={{font:'normal 34px Georgia,serif'}}>Shipping Address</h1>{[['fullName','Full Name'],['phone','Phone Number'],['line1','Address'],['city','City'],['state','State'],['pincode','Pincode'],['coupon','Coupon Code']].map(([k,l])=><label key={k}>{l}<input className="input" required={k!=='coupon'} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="button" type="submit">Continue to Payment</button>{status&&<p className="muted">{status}</p>}</form><aside className="summary"><h2>Payment</h2><p>Razorpay Checkout</p><p className="muted">UPI · Cards · Net Banking · Wallets</p><p><strong>COD</strong> is supported by the order model and can be enabled in admin settings.</p><hr/><Link href="/shipping-policy">Shipping & Returns →</Link></aside></div></div>}
