import Link from 'next/link';import {db} from '@/lib/db';import {requireAdmin} from '@/lib/auth';import {redirect} from 'next/navigation';
export default async function Automations(){try{await requireAdmin()}catch{redirect('/admin/login')}const rows=await db.automation.findMany({include:{runs:{orderBy:{createdAt:'desc'},take:3}},orderBy:{name:'asc'}});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/orders">Orders</Link><Link href="/admin/products">Products</Link><Link href="/admin/automations">Automations</Link></aside><section className="admin-main"><h1>Automation Engine</h1><p className="muted">Event-driven commerce workflows. Configure triggers/actions through the API and run the worker from your scheduler.</p><div className="feature-grid">{rows.map(a=><div className="feature" key={a.id}><h3>{a.name}</h3><p>Trigger: <strong>{a.trigger}</strong></p><p>{a.enabled?'Enabled':'Disabled'} · {a.runs.length} recent runs</p></div>)}</div>{!rows.length&&<div className="feature"><h3>No rules configured</h3><p>Create rules with POST /api/admin/automations.</p></div>}</section></div>}
