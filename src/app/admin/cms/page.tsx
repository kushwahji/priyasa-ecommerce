import Link from 'next/link';import {db} from '@/lib/db';import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';export default async function Cms(){const s=await getSession();if(!s||!['ADMIN','STAFF'].includes(s.role))redirect('/admin/login');const sections=await db.cmsSection.findMany({orderBy:{sortOrder:'asc'}});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/cms">CMS</Link><Link href="/admin/products">Products</Link><Link href="/admin/orders">Orders</Link><Link href="/admin/marketing">Marketing</Link></aside><section className="admin-main"><h1>CMS</h1><p className="muted">Control homepage sections, banners and content without code.</p><table className="table"><thead><tr><th>Section</th><th>Type</th><th>CTA</th><th>Active</th></tr></thead><tbody>{sections.map(x=><tr key={x.id}><td>{x.title||x.key}</td><td>{x.type}</td><td>{x.ctaLabel||'—'}</td><td>{x.active?'Yes':'No'}</td></tr>)}</tbody></table>{!sections.length&&<div className="empty">No CMS sections yet. Seed or create them through the CMS API when your production content is ready.</div>}</section></div>}
