'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function AdminLogin(){const [phone,setPhone]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});if(r.ok)router.push('/admin');else setError((await r.json()).error||'Login failed')}return <div className="page"><form className="form" style={{margin:'50px auto'}} onSubmit={submit}><h1 style={{font:'normal 34px Georgia,serif'}}>PRIYASA Admin</h1><p className="muted">Authorized staff only.</p><label>Phone<input className="input" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button">Sign In</button>{error&&<p style={{color:'#a00'}}>{error}</p>}</form></div>}
