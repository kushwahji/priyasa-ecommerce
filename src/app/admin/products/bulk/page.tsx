'use client';
import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';
type Row={id:string;sku:string;size:string;color:string;price:number|null;stock:number;reserved:number;product:{id:string;name:string;category:{name:string}}};
export default function BulkVariants(){
 const [rows,setRows]=useState<Row[]>([]),[selected,setSelected]=useState<string[]>([]),[q,setQ]=useState(''),[priceMode,setPriceMode]=useState('keep'),[priceValue,setPriceValue]=useState(''),[stockMode,setStockMode]=useState('keep'),[stockValue,setStockValue]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);
 async function load(){const r=await fetch('/api/admin/products',{cache:'no-store'});const d=await r.json();if(r.ok)setRows((d.data||[]).flatMap((p:any)=>(p.variants||[]).map((v:any)=>({...v,product:{id:p.id,name:p.name,category:p.category}})));else setMessage(d.error||'Unable to load variants')}
 useEffect(()=>{load()},[]);
 const filtered=useMemo(()=>{const s=q.trim().toLowerCase();return s?rows.filter(r=>[r.sku,r.size,r.color,r.product.name,r.product.category.name].join(' ').toLowerCase().includes(s)):rows},[rows,q]);
 const toggle=(id:string)=>setSelected(x=>x.includes(id)?x.filter(v=>v!==id):[...x,id]);
 const allVisible=filtered.length>0&&filtered.every(x=>selected.includes(x.id));
 async function apply(){if(!selected.length)return setMessage('Select at least one variant.');setBusy(true);setMessage('Updating variants…');try{const r=await fetch('/api/admin/variants/bulk',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({variantIds:selected,priceMode,priceValue:priceValue===''?undefined:Number(priceValue),stockMode,stockValue:stockValue===''?undefined:Number(stockValue)})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Bulk update failed');setMessage(`${d.updated} variants updated successfully.`);setSelected([]);await load()}catch(e){setMessage(e instanceof Error?e.message:'Bulk update failed')}finally{setBusy(false)}}
 return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA Admin</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/products">Products</Link><Link href="/admin/products/bulk">Bulk variants</Link><Link href="/admin/inventory">Inventory</Link><Link href="/admin/categories">Categories</Link></aside><section className="admin-main"><div className="section-head"><div><span className="eyebrow dark">CATALOG OPERATIONS</span><h1>Bulk Variant Updates</h1><p className="muted">Select SKUs and update prices or stock in one operation. Reserved stock is never reduced below the reserved quantity.</p></div><Link className="button" href="/admin/products">Back to products</Link></div>{message&&<div className="checkout-status">{message}</div>}<div className="admin-card bulk-toolbar"><div className="admin-form-grid"><label>Search SKU / product / size / color<input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="e.g. XL, pink, SKU…"/></label><label>Selected<strong style={{display:'block',fontSize:24,marginTop:7}}>{selected.length}</strong></label><label>Price action<select className="input" value={priceMode} onChange={e=>setPriceMode(e.target.value)}><option value="keep">No price change</option><option value="set">Set exact price</option><option value="increase-percent">Increase by %</option><option value="decrease-percent">Decrease by %</option></select></label><label>Price value<input className="input" type="number" min="0" value={priceValue} onChange={e=>setPriceValue(e.target.value)} placeholder="₹ / %" disabled={priceMode==='keep'}/></label><label>Stock action<select className="input" value={stockMode} onChange={e=>setStockMode(e.target.value)}><option value="keep">No stock change</option><option value="set">Set exact stock</option><option value="increase">Add stock</option><option value="decrease">Reduce stock</option></select></label><label>Stock value<input className="input" type="number" min="0" value={stockValue} onChange={e=>setStockValue(e.target.value)} placeholder="Units" disabled={stockMode==='keep'}/></label></div><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><button className="button dark-button" disabled={busy||!selected.length} onClick={apply}>{busy?'Updating…':'Apply to selected'}</button><button className="button" onClick={()=>setSelected([])}>Clear selection</button></div></div><div className="admin-card" style={{marginTop:18}}><div className="table-wrap"><table className="table"><thead><tr><th><input type="checkbox" checked={allVisible} onChange={()=>setSelected(allVisible?selected.filter(id=>!filtered.some(r=>r.id===id)):[...new Set([...selected,...filtered.map(r=>r.id)])]}/></th><th>Product</th><th>SKU</th><th>Attributes</th><th>Price</th><th>Stock</th><th>Reserved</th><th>Available</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td><input type="checkbox" checked={selected.includes(r.id)} onChange={()=>toggle(r.id)}/></td><td><strong>{r.product.name}</strong><br/><small>{r.product.category.name}</small></td><td>{r.sku}</td><td>{r.size} · {r.color}</td><td>₹{(r.price??0).toLocaleString('en-IN')}</td><td>{r.stock}</td><td>{r.reserved}</td><td><strong>{Math.max(0,r.stock-r.reserved)}</strong></td></tr>)}</tbody></table>{!filtered.length&&<div className="empty">No variants match the filter.</div>}</div></div></section></div>}
