import Link from 'next/link';
import { db } from '@/lib/db';
import { getSession } from '@/lib/auth';
import { redirect } from 'next/navigation';
import { money } from '@/lib/catalog';
export default async function ProductsAdmin(){const s=await getSession();if(!s||!['ADMIN','STAFF'].includes(s.role))redirect('/admin/login');const rows=await db.product.findMany({include:{category:true,variants:true},orderBy:{updatedAt:'desc'}});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/orders">Orders</Link><Link href="/admin/products">Products</Link><Link href="/admin/cms">CMS</Link><Link href="/admin/marketing">Marketing</Link><Link href="/admin/settings">Settings</Link></aside><section className="admin-main"><div className="section-head"><div><h1>Products</h1><p className="muted">Catalog, variants, price and stock.</p></div><Link className="button" href="/admin/products/new">+ Add Product</Link></div><table className="table"><thead><tr><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><strong>{p.name}</strong><br/><small>{p.slug}</small></td><td>{p.category.name}</td><td>{money(p.salePrice)}</td><td>{p.variants.reduce((s,v)=>s+v.stock-v.reserved,0)}</td><td>{p.active?'Live':'Draft'}</td></tr>)}</tbody></table></section></div>}
