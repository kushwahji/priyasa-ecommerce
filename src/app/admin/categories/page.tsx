import Link from 'next/link';import {redirect} from 'next/navigation';import {db} from '@/lib/db';import {getSession} from '@/lib/auth';
export default async function CategoriesAdmin(){const s=await getSession();if(!s||!['ADMIN','STAFF'].includes(s.role))redirect('/admin/login');const rows=await db.category.findMany({include:{_count:{select:{products:true}}},orderBy:{name:'asc'}});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA Admin</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/products">Products</Link><Link href="/admin/categories">Categories</Link><Link href="/admin/coupons-offers">Offers & Coupons</Link></aside><section className="admin-main"><div className="section-head"><div><h1>Categories</h1><p className="muted">Live database categories used by the storefront.</p></div></div><form action="/api/admin/categories" method="post" style={{display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gap:10,marginBottom:24}}><input className="input" name="name" placeholder="Category name" required/><input className="input" name="slug" placeholder="category-slug" required/><input className="input" name="imageUrl" placeholder="Image URL"/><button className="button" type="submit">Add category</button></form><table className="table"><thead><tr><th>Name</th><th>Slug</th><th>Products</th><th>Created</th></tr></thead><tbody>{rows.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.slug}</td><td>{c._count.products}</td><td>{c.createdAt.toLocaleDateString('en-IN')}</td></tr>)}</tbody></table></section></div>}
