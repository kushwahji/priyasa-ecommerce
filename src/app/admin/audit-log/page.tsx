import Link from 'next/link';import {redirect} from 'next/navigation';import {db} from '@/lib/db';import {getSession} from '@/lib/auth';
export default async function AuditLog(){const s=await getSession();if(!s||s.role!=='ADMIN')redirect('/admin/login');const rows=await db.auditLog.findMany({include:{user:{select:{name:true,phone:true}}},orderBy:{createdAt:'desc'},take:300});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA Admin</h2><Link href="/admin">Dashboard</Link><Link href="/admin/settings">Settings</Link><Link href="/admin/audit-log">Audit Log</Link></aside><section className="admin-main"><h1>Audit Log</h1><p className="muted">Immutable operational trail for privileged commerce actions.</p><table className="table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Entity ID</th><th>Metadata</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.createdAt.toLocaleString('en-IN')}</td><td>{x.user?.name||x.user?.phone||'System'}</td><td><strong>{x.action}</strong></td><td>{x.entity}</td><td><small>{x.entityId||'—'}</small></td><td><small>{x.metadata?JSON.stringify(x.metadata):'—'}</small></td></tr>)}</tbody></table></section></div>}
