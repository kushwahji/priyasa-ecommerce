import Link from 'next/link';import {redirect} from 'next/navigation';import {db} from '@/lib/db';import {getSession} from '@/lib/auth';
export default async function Seo(){const s=await getSession();if(!s||!['ADMIN','STAFF'].includes(s.role))redirect('/admin/login');const products=await db.product.findMany({select:{id:true,name:true,slug:true,seoTitle:true,seoDescription:true,noIndex:true,active:true},orderBy:{updatedAt:'desc'},take:200});const pages=await db.page.findMany({select:{slug:true,title:true,seoTitle:true,seoDescription:true,published:true},orderBy:{slug:'asc'}});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA Admin</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/seo">SEO</Link><Link href="/admin/cms">CMS</Link><Link href="/admin/products">Products</Link></aside><section className="admin-main"><h1>SEO</h1><p className="muted">Audit live product and content metadata. Product editing controls can update SEO fields from the catalog workflow.</p><h2>Products</h2><table className="table"><thead><tr><th>Product</th><th>SEO title</th><th>Description</th><th>Index</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}<br/><small>{p.slug}</small></td><td>{p.seoTitle||<span className="muted">Missing</span>}</td><td>{p.seoDescription||<span className="muted">Missing</span>}</td><td>{p.noIndex?'No':'Yes'}</td></tr>)}</tbody></table><h2 style={{marginTop:30}}>Pages</h2><table className="table"><thead><tr><th>Slug</th><th>Title</th><th>SEO title</th><th>Published</th></tr></thead><tbody>{pages.map(p=><tr key={p.slug}><td>{p.slug}</td><td>{p.title}</td><td>{p.seoTitle||'Missing'}</td><td>{p.published?'Yes':'Draft'}</td></tr>)}</tbody></table></section></div>}
