import Link from 'next/link';import {db} from '@/lib/db';import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {money} from '@/lib/catalog';import AdminOrderActions from '@/components/AdminOrderActions';
export default async function OrdersAdmin(){const s=await getSession();if(!s||!['ADMIN','STAFF'].includes(s.role))redirect('/admin/login');const rows=await db.order.findMany({include:{user:true,payment:true,items:true,shipment:true},orderBy:{createdAt:'desc'},take:100});return <div className="admin-shell"><aside className="admin-nav"><h2>PRIYASA</h2><Link href="/admin">← Dashboard</Link><Link href="/admin/orders">Orders</Link><Link href="/admin/products">Products</Link><Link href="/admin/inventory">Inventory</Link><Link href="/admin/cms">CMS</Link><Link href="/admin/marketing">Marketing</Link></aside><section className="admin-main"><h1>Orders</h1><p className="muted">Operational order queue: payments, fulfillment, shipment and refunds.</p><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Total</th><th>Payment</th><th>Fulfillment</th><th>Operations</th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td>{o.orderNumber}<br/><small>{o.createdAt.toLocaleString('en-IN')}</small></td><td>{o.user?.name||'Guest'}<br/>{o.user?.phone}</td><td>{o.items.length}</td><td>{money(o.total)}</td><td>{o.payment?.status||'—'}</td><td>{o.fulfillmentStatus}<br/>{o.shipment?.trackingNumber||'No AWB'}</td><td><AdminOrderActions orderId={o.id} status={o.status} hasShipment={!!o.shipment} paymentStatus={o.payment?.status||''} total={o.total}/></td></tr>)}</tbody></table></section></div>}
