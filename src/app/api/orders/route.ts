import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {Prisma} from '@prisma/client';import {z} from 'zod';import {db} from '@/lib/db';
const schema=z.object({phone:z.string().regex(/^\+?\d{10,15}$/),fullName:z.string().min(2).max(120),line1:z.string().min(3).max(250),city:z.string().min(2).max(80),state:z.string().min(2).max(80),pincode:z.string().regex(/^\d{6}$/),addressId:z.string().optional(),items:z.array(z.object({variantId:z.string(),quantity:z.number().int().min(1).max(20)})).min(1).max(50),coupon:z.string().trim().max(40).optional()});
export async function POST(req:Request){const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:'Invalid checkout data',details:parsed.error.flatten()},{status:400});const data=parsed.data;const jar=await cookies();const localUserId=jar.get('priyasa_local_user_id')?.value;const mobile=jar.get('priyasa_mobile')?.value;if(!localUserId||!mobile)return NextResponse.json({error:'Please sign in with your mobile number before checkout.'},{status:401});if(data.phone.replace(/\D/g,'').slice(-10)!==mobile.replace(/\D/g,'').slice(-10))return NextResponse.json({error:'Checkout phone does not match the signed-in customer.'},{status:403});const key=req.headers.get('idempotency-key')||crypto.randomUUID();const hash=JSON.stringify(data);const existing=await db.idempotencyKey.findUnique({where:{key}});if(existing){if(existing.scope!=='checkout'||existing.requestHash!==requireHash(hash))return NextResponse.json({error:'Idempotency key was reused for different checkout data'},{status:409});if(existing.response)return NextResponse.json(existing.response);}
try{const result=await db.$transaction(async tx=>{const user=await tx.user.findUnique({where:{id:localUserId}});if(!user)throw new Error('CUSTOMER_NOT_FOUND');const lock=await tx.idempotencyKey.create({data:{key,scope:'checkout',requestHash:requireHash(hash),expiresAt:new Date(Date.now()+86400000)}}).catch(()=>null);if(!lock){const prior=await tx.idempotencyKey.findUnique({where:{key}});if(prior?.response)return prior.response as any;throw new Error('IDEMPOTENCY_IN_PROGRESS');}const ids=[...new Set(data.items.map(i=>i.variantId))];const variants=await tx.productVariant.findMany({where:{id:{in:ids}},include:{product:true}});if(variants.length!==ids.length)throw new Error('One or more products are unavailable');const lines=data.items.map(i=>({i,v:variants.find(x=>x.id===i.variantId)!}));for(const x of lines){const reserved=await tx.$executeRaw(Prisma.sql`UPDATE ProductVariant SET reserved = reserved + ${x.i.quantity} WHERE id = ${x.v.id} AND (stock - reserved) >= ${x.i.quantity}`);if(reserved!==1)throw new Error(`${x.v.product.name} is no longer available`);}const subtotal=lines.reduce((s,x)=>s+(x.v.price??x.v.product.salePrice)*x.i.quantity,0);let discount=0;let couponCode:string|undefined;if(data.coupon){const c=await tx.coupon.findUnique({where:{code:data.coupon.toUpperCase()}});const now=new Date();if(c&&c.active&&now>=c.startsAt&&now<=c.endsAt&&subtotal>=c.minCart&&(!c.maxUses||c.usedCount<c.maxUses)){couponCode=c.code;discount=c.type==='PERCENTAGE'?Math.floor(subtotal*c.value/100):Math.min(c.value,subtotal);}}const shipping=subtotal-discount>=999?0:99;const total=Math.max(0,subtotal-discount)+shipping;let address;if(data.addressId){address=await tx.address.findFirst({where:{id:data.addressId,userId:user.id}});if(!address)throw new Error('Saved address not found');}else address=await tx.address.create({data:{userId:user.id,fullName:data.fullName,phone:user.phone,line1:data.line1,city:data.city,state:data.state,pincode:data.pincode,isDefault:false}});const order=await tx.order.create({data:{orderNumber:`PRI-${Date.now().toString(36).toUpperCase()}-${Math.floor(Math.random()*999).toString().padStart(3,'0')}`,userId:user.id,addressId:address.id,status:'PAYMENT_PENDING',fulfillmentStatus:'UNFULFILLED',subtotal,discount,shipping,tax:0,total,couponCode,items:{create:lines.map(x=>({variantId:x.v.id,productName:x.v.product.name,sku:x.v.sku,size:x.v.size,color:x.v.color,unitPrice:x.v.price??x.v.product.salePrice,quantity:x.i.quantity}))},payment:{create:{provider:'razorpay',amount:total,status:'PENDING'}}},include:{payment:true}});for(const x of lines)await tx.inventoryMovement.create({data:{variantId:x.v.id,quantity:x.i.quantity,reason:'RESERVE',referenceId:order.id}});await tx.orderStatusHistory.create({data:{orderId:order.id,toStatus:'PAYMENT_PENDING',note:'Checkout order created'}});const response={orderId:order.id,orderNumber:order.orderNumber,total:order.total,paymentId:order.payment?.id};await tx.idempotencyKey.update({where:{key},data:{orderId:order.id,response}});return response;},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable,maxWait:5000,timeout:15000});return NextResponse.json(result,{status:201});}catch(e){const prior=await db.idempotencyKey.findUnique({where:{key}}).catch(()=>null);if(prior?.response)return NextResponse.json(prior.response);return NextResponse.json({error:e instanceof Error?e.message:'Unable to create order'},{status:409});}}
function requireHash(value:string){return value;}
