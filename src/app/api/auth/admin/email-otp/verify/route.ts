import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {db} from '@/lib/db';import {setSession} from '@/lib/auth';
export async function POST(req:Request){const {email,code,challengeId}=await req.json().catch(()=>({}));if(typeof email!=='string'||typeof code!=='string'||typeof challengeId!=='string')return NextResponse.json({error:'Email, code and challenge are required'},{status:400});const c=await db.emailOtpChallenge.findUnique({where:{id:challengeId}});if(!c||c.email!==email.toLowerCase().trim()||c.consumedAt||c.expiresAt<new Date())return NextResponse.json({error:'Code expired or invalid'},{status:401});if(c.attempts>=5)return NextResponse.json({error:'Too many attempts'},{status:429});const ok=await bcrypt.compare(code,c.codeHash);if(!ok){await db.emailOtpChallenge.update({where:{id:c.id},data:{attempts:{increment:1}}});return NextResponse.json({error:'Invalid verification code'},{status:401});}const user=await db.user.findUnique({where:{email:c.email}});if(!user||!['ADMIN','STAFF'].includes(user.role))return NextResponse.json({error:'Admin account not found'},{status:403});await db.$transaction([db.emailOtpChallenge.update({where:{id:c.id},data:{consumedAt:new Date()}}),db.auditLog.create({data:{userId:user.id,action:'ADMIN_EMAIL_OTP_LOGIN',entity:'User',entityId:user.id}})]);await setSession(user);return NextResponse.json({ok:true,role:user.role});}
