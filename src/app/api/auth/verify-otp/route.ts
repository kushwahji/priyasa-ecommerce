import {NextResponse} from 'next/server';
import {cookies} from 'next/headers';
import {priyasaApi,apiError} from '@/lib/priyasa-api';
import {db} from '@/lib/db';

export async function POST(req:Request){try{const input=await req.json();const {response,body:result}=await priyasaApi('/api/v1/auth/verify-otp',{method:'POST',body:JSON.stringify(input)});if(!response.ok)return NextResponse.json(result,{status:response.status});const data=(result as {data?:{token?:string;token_type?:string;user?:{id?:number;mobile?:string;name?:string}}})?.data;const phone=String(data?.user?.mobile||input.mobile||'').replace(/\D/g,'').slice(-10);let localUserId='';if(phone.length===10){const user=await db.user.upsert({where:{phone},update:{name:data?.user?.name||undefined},create:{phone,name:data?.user?.name||undefined}});localUserId=user.id;}if(data?.token){const jar=await cookies();jar.set('priyasa_access_token',data.token,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:60*60*24*30});if(data.user?.id)jar.set('priyasa_user_id',String(data.user.id),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:60*60*24*30});if(phone)jar.set('priyasa_mobile',phone,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:60*60*24*30});if(localUserId)jar.set('priyasa_local_user_id',localUserId,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:60*60*24*30});}return NextResponse.json(result);}catch(error){return NextResponse.json({success:false,message:apiError(error,'Unable to verify OTP')},{status:502});}}
