import {NextResponse} from 'next/server';import {z} from 'zod';import {db} from '@/lib/db';import {requireAdminPermission} from '@/lib/auth';
const schema=z.object({key:z.string().min(2).max(100),type:z.string().min(2).max(50),title:z.string().max(180).optional(),subtitle:z.string().max(500).optional(),imageUrl:z.string().url().optional().or(z.literal('')),mobileImageUrl:z.string().url().optional().or(z.literal('')),ctaLabel:z.string().max(80).optional(),ctaHref:z.string().max(200).optional(),startsAt:z.string().optional(),endsAt:z.string().optional(),active:z.boolean().default(true),sortOrder:z.number().int().default(0)});
export async function POST(req:Request){try{const s=await requireAdminPermission('content.write');const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid CMS content',details:p.error.flatten()},{status:400});const x=p.data;const section=await db.cmsSection.upsert({where:{key:x.key},update:{type:x.type,title:x.title||null,subtitle:x.subtitle||null,imageUrl:x.imageUrl||null,mobileImageUrl:x.mobileImageUrl||null,ctaLabel:x.ctaLabel||null,ctaHref:x.ctaHref||null,startsAt:x.startsAt?new Date(x.startsAt):null,endsAt:x.endsAt?new Date(x.endsAt):null,active:x.active,sortOrder:x.sortOrder},create:{key:x.key,type:x.type,title:x.title||null,subtitle:x.subtitle||null,imageUrl:x.imageUrl||null,mobileImageUrl:x.mobileImageUrl||null,ctaLabel:x.ctaLabel||null,ctaHref:x.ctaHref||null,startsAt:x.startsAt?new Date(x.startsAt):null,endsAt:x.endsAt?new Date(x.endsAt):null,active:x.active,sortOrder:x.sortOrder}});await db.auditLog.create({data:{userId:s.userId,action:'UPSERT',entity:'CmsSection',entityId:section.id}});return NextResponse.json({data:section});}catch(e){return NextResponse.json({error:e instanceof Error&&e.message.includes('FORBIDDEN')?'Forbidden':'Unable to save CMS section'},{status:e instanceof Error&&e.message.includes('FORBIDDEN')?403:500});}}
