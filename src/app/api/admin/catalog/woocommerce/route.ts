import {NextResponse} from 'next/server';import {requireAdminPermission} from '@/lib/auth';import {db} from '@/lib/db';import {importWooCommerceCatalog,previewWooCommerceImport,syncWooCommerceCatalog} from '@/lib/woocommerce-import';
export async function GET(){try{await requireAdminPermission('products.read');return NextResponse.json({data:await previewWooCommerceImport()});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to connect to WooCommerce'},{status:500});}}
export async function POST(req:Request){try{const session=await requireAdminPermission('products.write');const body=await req.json().catch(()=>({}));if(body?.mode==='preview')return NextResponse.json({data:await previewWooCommerceImport()});const mode=body?.mode==='sync'?'sync':'import';const result=mode==='sync'?await syncWooCommerceCatalog():await importWooCommerceCatalog();await db.auditLog.create({data:{userId:session.userId,action:mode==='sync'?'SYNC':'IMPORT',entity:'WooCommerceCatalog',metadata:{mode,...result}}});return NextResponse.json({data:{mode,...result}});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'WooCommerce catalog operation failed'},{status:500});}}
