import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/auth';import {db} from '@/lib/db';
export async function GET(){try{await requireAdmin();return NextResponse.json({data:await db.automation.findMany({include:{runs:{orderBy:{createdAt:'desc'},take:5}},orderBy:{name:'asc'}})});}catch{return NextResponse.json({error:'Forbidden'},{status:403});}}
export async function POST(req:Request){try{const s=await requireAdmin();const b=await req.json();if(typeof b.name!=='string'||typeof b.trigger!=='string'||!Array.isArray(b.actions))return NextResponse.json({error:'name, trigger and actions required'},{status:400});const a=await db.automation.create({data:{name:b.name,trigger:b.trigger,conditions:b.conditions||undefined,actions:b.actions,enabled:b.enabled!==false}});await db.auditLog.create({data:{userId:s.userId,action:'AUTOMATION_CREATED',entity:'Automation',entityId:a.id}});return NextResponse.json({data:a},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to create automation'},{status:500});}}
