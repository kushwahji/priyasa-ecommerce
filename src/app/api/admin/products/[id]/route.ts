import {NextResponse} from 'next/server';import {z} from 'zod';import {db} from '@/lib/db';import {requireAdmin} from '@/lib/auth';
const schema=z.object({name:z.string().min(2).max(200).optional(),slug:z.string().regex(/^[a-z0-9-]+$/).optional(),description:z.string().min(5).optional(),categoryId:z.string().optional(),mrp:z.number().int().positive().optional(),salePrice:z.number().int().positive().optional(),active:z.boolean().optional(),seoTitle:z.string().optional(),seoDescription:z.string().optional(),noIndex:z.boolean().optional()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const s=await requireAdmin();const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid product'},{status:400});const product=await db.product.update({where:{id},data:p.data});await db.auditLog.create({data:{userId:s.userId,action:'UPDATE',entity:'Product',entityId:id,metadata:p.data}});return NextResponse.json({data:product});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Forbidden'},{status:403});}}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){try{const s=await requireAdmin();const {id}=await params;const product=await db.product.update({where:{id},data:{active:false}});await db.auditLog.create({data:{userId:s.userId,action:'ARCHIVE',entity:'Product',entityId:id}});return NextResponse.json({data:product});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Forbidden'},{status:403});}}
