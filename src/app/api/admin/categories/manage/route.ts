import {NextResponse} from 'next/server';import {z} from 'zod';import {db} from '@/lib/db';import {requireAdminPermission} from '@/lib/auth';
const schema=z.object({id:z.string().min(1),name:z.string().min(2).max(100),slug:z.string().regex(/^[a-z0-9-]+$/).max(100),description:z.string().max(500).optional(),imageUrl:z.string().url().optional().or(z.literal('')),parentId:z.string().optional().or(z.literal(''))});
export async function PATCH(req:Request){try{const s=await requireAdminPermission('products.write');const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid category',details:p.error.flatten()},{status:400});if(p.data.parentId===p.data.id)return NextResponse.json({error:'A category cannot be its own parent'},{status:400});const c=await db.category.update({where:{id:p.data.id},data:{name:p.data.name,slug:p.data.slug,description:p.data.description||null,imageUrl:p.data.imageUrl||null,parentId:p.data.parentId||null}});await db.auditLog.create({data:{userId:s.userId,action:'UPDATE',entity:'Category',entityId:c.id}});return NextResponse.json({data:c})}catch(e){const m=e instanceof Error?e.message:'';return NextResponse.json({error:m.includes('Unique constraint')?'Category slug already exists':m.includes('FORBIDDEN')?'Forbidden':'Unable to update category'},{status:m.includes('FORBIDDEN')?403:409})}}
export async function DELETE(req:Request){try{const s=await requireAdminPermission('products.write');const id=new URL(req.url).searchParams.get('id')||'';const c=await db.category.findUnique({where:{id},include:{_count:{select:{products:true,children:true}}});if(!c)return NextResponse.json({error:'Category not found'},{status:404});if(c._count.products||c._count.children)return NextResponse.json({error:'Move products and child categories before deleting this category.'},{status:409});await db.category.delete({where:{id}});await db.auditLog.create({data:{userId:s.userId,action:'DELETE',entity:'Category',entityId:id}});return NextResponse.json({ok:true})}catch(e){const m=e instanceof Error?e.message:'';return NextResponse.json({error:m.includes('FORBIDDEN')?'Forbidden':'Unable to delete category'},{status:m.includes('FORBIDDEN')?403:409})}}
