import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {authenticateCustomApi,finishCustomApi} from '@/lib/custom-api-auth';import {serializeProduct} from '@/lib/custom-api-orders';
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const a=await authenticateCustomApi(req,'products.read');if(a.response)return a.response;const {id}=await params;const p=await db.product.findUnique({where:{id},include:{variants:true,images:true,category:true}});if(!p){await finishCustomApi(req,a.connection,404);return NextResponse.json({success:false,error:'Product not found'},{status:404});}await finishCustomApi(req,a.connection,200);return NextResponse.json({success:true,data:await serializeProduct(p)});}
