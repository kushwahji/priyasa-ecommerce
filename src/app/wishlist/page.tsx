'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {money} from '@/lib/catalog';
export default function Wishlist(){const [items,setItems]=useState<any[]>([]),[auth,setAuth]=useState(true),[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/customer/wishlist',{cache:'no-store'}).then(r=>r.json()).then(d=>{setAuth(!!d.authenticated);setItems(d.data||[])}).finally(()=>setLoading(false))},[]);if(loading)return <div className="page"><div className="account-orders-loading">Loading wishlist…</div></div>;if(!auth)return <div className="page"><div className="order-empty account-card"><h2>Sign in to save your wishlist</h2><p>Your wishlist follows you across devices when you sign in.</p><Link className="button" href="/login">Sign in with mobile</Link></div></div>;return <div className="page storefront-inner"><div className="breadcrumbs"><Link href="/">Home</Link><span> / </span> Wishlist</div><div className="collection-intro"><div><span className="eyebrow dark">SAVED FOR YOU</span><h2>My Wishlist</h2></div><span>{items.length} saved styles</span></div>{items.length?<div className="product-grid shop">{items.map(p=><article className="product-card" key={p.id}><div className="product-media"><Link href={`/product/${p.slug}`}><img src={p.image||'/images/product-placeholder.svg'} alt={p.name}/></Link></div><div className="product-info"><div className="product-category">{p.category}</div><h3><Link href={`/product/${p.slug}`}>{p.name}</Link></h3><strong>{money(p.price)}</strong></div></article>)}</div>:<div className="order-empty account-card"><div className="order-empty-icon">♡</div><h2>Your wishlist is empty</h2><p>Save pieces you love and come back to them anytime.</p><Link className="button" href="/shop">Explore Styles</Link></div>}</div>}
