'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
const money=(n:number)=>`₹${n.toLocaleString('en-IN')}`;
const statusLabel=(s:string)=>s.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());
export default function Orders(){const [orders,setOrders]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch('/api/customer/orders',{cache:'no-store'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load orders');setOrders(d.data||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);if(loading)return <div className="account-shell"><div className="account-orders-loading">Loading your orders…</div></div>;if(error)return <div className="account-shell"><div className="account-card order-empty"><h2>Sign in to view your orders</h2><p>{error}</p><Link className="button" href="/login">Sign in with mobile</Link></div></div>;return <div className="account-shell"><div className="breadcrumbs"><Link href="/">Home</Link><span> / </span><Link href="/account">My Account</Link><span> / </span> My Orders</div><div className="account-main-head"><div><span className="eyebrow">MY PRIYASA</span><h1>My Orders</h1></div><Link className="button button-light" href="/shop">Continue Shopping</Link></div>{!orders.length?<div className="account-card order-empty"><div className="order-empty-icon">♡</div><h2>No orders yet</h2><p>When you place your first Priyasa order, it will appear here.</p><Link className="button" href="/shop">Start Shopping</Link></div>:<div className="orders-list">{orders.map(o=><article className="order-card" key={o.id}><header><div><strong>Order {o.orderNumber}</strong><small>{new Date(o.createdAt).toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'})}</small></div><span className={`order-status status-${String(o.status).toLowerCase()}`}>{statusLabel(o.status)}</span></header><div className="order-items">{o.items.map((i:any)=><div className="order-item" key={i.id}><div className="order-item-image">{i.variant?.product?.images?.[0]?.url&&<img src={i.variant.product.images[0].url} alt=""/>}</div><div><strong>{i.productName}</strong><small>{i.color} · {i.size} · Qty {i.quantity}</small><span>{money(i.unitPrice*i.quantity)}</span></div></div>)}</div><footer><div><small>Total</small><strong>{money(o.total)}</strong></div><div className="order-actions"><Link className="button button-light" href={`/track-order?order=${encodeURIComponent(o.orderNumber)}`}>Track Order</Link><Link className="button" href={`/account/orders/${o.id}`}>View Details</Link></div></footer></article>)}</div>}</div>}
