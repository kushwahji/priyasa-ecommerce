'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import ReturnRequest from '@/components/ReturnRequest';
const money=(n:number)=>`₹${n.toLocaleString('en-IN')}`;const label=(s:string)=>s.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());
export default function OrderDetail({params}:{params:Promise<{id:string}>}){const [order,setOrder]=useState<any>();const [error,setError]=useState('');useEffect(()=>{params.then(p=>fetch(`/api/customer/orders/${p.id}`,{cache:'no-store'})).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load order');setOrder(d.data)}).catch(e=>setError(e.message))},[params]);if(error)return <div className="account-shell"><div className="account-card"><h2>{error}</h2><Link className="button" href="/login">Sign in</Link></div></div>;if(!order)return <div className="account-shell"><div className="account-orders-loading">Loading order…</div></div>;const returnExisting=order.returns?.[0]||null;const eligible=order.status==='DELIVERED'&&!returnExisting&&(Date.now()-new Date(order.createdAt).getTime()<30*24*60*60*1000);return <div className="account-shell"><div className="breadcrumbs"><Link href="/account/orders">My Orders</Link><span> / </span> {order.orderNumber}</div><div className="account-main-head"><div><span className="eyebrow">ORDER DETAILS</span><h1>{order.orderNumber}</h1></div><span className="order-status">{label(order.status)}</span></div><div className="order-detail-grid"><section className="account-card"><h2>Items</h2>{order.items.map((i:any)=><div className="detail-item" key={i.id}><div className="order-item-image">{i.variant?.product?.images?.[0]?.url&&<img src={i.variant.product.images[0].url} alt=""/>}</div><div><strong>{i.productName}</strong><small>{i.color} · {i.size} · Qty {i.quantity}</small><b>{money(i.unitPrice*i.quantity)}</b></div></div>)}</section><aside className="account-card"><h2>Order summary</h2><div className="summary-lines"><div><span>Subtotal</span><strong>{money(order.subtotal)}</strong></div><div><span>Discount</span><strong>-{money(order.discount)}</strong></div><div><span>Shipping</span><strong>{order.shipping?money(order.shipping):'Free'}</strong></div><hr/><div className="cart-total"><span>Total</span><strong>{money(order.total)}</strong></div></div>{order.address&&<div className="order-address"><strong>Delivery address</strong><p>{order.address.fullName}<br/>{order.address.line1}<br/>{order.address.city}, {order.address.state} - {order.address.pincode}</p></div>}</aside></div><section className="account-card order-timeline"><h2>Order timeline</h2>{order.statusHistory.map((h:any)=><div key={h.id} className="timeline-row"><b>✓</b><div><strong>{label(h.toStatus)}</strong><small>{new Date(h.createdAt).toLocaleString('en-IN')}</small>{h.note&&<span>{h.note}</span>}</div></div>)}{order.shipment&&<div className="shipment-box"><strong>Shipment</strong><p>{order.shipment.carrier||'Carrier assigned'} {order.shipment.trackingNumber?`· ${order.shipment.trackingNumber}`:''}</p>{order.shipment.trackingUrl&&<a href={order.shipment.trackingUrl} target="_blank" rel="noreferrer">Track shipment →</a>}</div>}<ReturnRequest orderId={order.id} eligible={eligible} existing={returnExisting}/></section></div>}
