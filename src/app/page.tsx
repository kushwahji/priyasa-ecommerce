import {getBestSellers,getHomeCms,getLatestLaunches,getProductsForHomeSection,getStorefrontCategories,getStorefrontProducts} from '@/lib/storefront-data';
import {ProductCard} from '@/components/ProductCard';import HomeHeroCarousel from '@/components/HomeHeroCarousel';import HomeImageCarousel from '@/components/HomeImageCarousel';import HomeCmsSection from '@/components/HomeCmsSection';import AffiliateHomeSection from '@/components/AffiliateHomeSection';import {TruckIcon,ShieldIcon,ReturnIcon,GiftIcon} from '@/components/StorefrontIcons';
export const dynamic='force-dynamic';
const productSectionType=(type:string)=>{const t=type.toLowerCase();return t.startsWith('products-')||['latest','latest-collection','best-sellers','trending','sale'].includes(t)};
const festivalSlug=(categories:any[])=>categories.find(c=>['festival','festive','diwali','navratri','eid','wedding'].some(k=>c.slug.toLowerCase().includes(k)))?.slug;
export default async function Home(){const [fallbackBest,categories,sections,latest,saleProducts]=await Promise.all([getBestSellers(4),getStorefrontCategories(),getHomeCms(),getLatestLaunches(8),getProductsForHomeSection('products-sale',8)]);const festival=festivalSlug(categories);const festivalProducts=festival?await getStorefrontProducts({categorySlug:festival,limit:8}):[];const heroSlides=sections.filter(s=>['hero','hero-slide'].includes(s.type.toLowerCase()));const imageSlides=sections.filter(s=>['image-carousel','image-slide','carousel'].includes(s.type.toLowerCase()));const productSections=sections.filter(s=>productSectionType(s.type));const sectionProducts=await Promise.all(productSections.map(s=>getProductsForHomeSection(s.type,8)));const promos=sections.filter(s=>['promo','banner','image-banner','collection-banner'].includes(s.type.toLowerCase()));const features=sections.filter(s=>['feature','lifestyle'].includes(s.type.toLowerCase()));const customSections=sections.filter(s=>['category-grid','text'].includes(s.type.toLowerCase()));const hasCmsCategoryGrid=customSections.some(s=>s.type.toLowerCase()==='category-grid');const hasCmsProducts=productSections.length>0;const heroProduct=latest[0];return <>{heroSlides.length>0?<HomeHeroCarousel slides={heroSlides}/>:heroProduct?<section className="hero hero-editorial hero-fallback" style={{backgroundImage:`linear-gradient(90deg,rgba(255,255,255,.96) 0%,rgba(255,255,255,.82) 42%,rgba(255,255,255,0) 72%),url(${heroProduct.image})`}}><div className="hero-copy"><span className="eyebrow">LATEST FROM PRIYASA</span><h1>{heroProduct.name}</h1><p>{heroProduct.description||'Discover the latest style from the live Priyasa catalog.'}</p><a className="button" href={`/product/${heroProduct.slug}`}>Shop This Style →</a></div></section>:null}{(heroSlides.length>0||heroProduct)&&<section className="trust-strip"><span><TruckIcon/><b>Free Shipping</b><small>Live store policy</small></span><i/><span><GiftIcon/><b>COD Available</b><small>At eligible pincodes</small></span><i/><span><ReturnIcon/><b>Easy Returns</b><small>As per return policy</small></span><i/><span><ShieldIcon/><b>Secure Payment</b><small>Payment protected checkout</small></span></section>}{hasCmsProducts&&productSections.map((s,index)=>{const products=sectionProducts[index]||[];if(!products.length)return null;return <section className="section section-tight home-product-section" key={s.id}><div className="section-head"><div><span className="eyebrow dark">{s.subtitle||'PRIYASA EDIT'}</span><h2>{s.title||'Curated for you'}</h2></div>{s.ctaHref&&<a className="text-link" href={s.ctaHref}>{s.ctaLabel||'View All'} →</a>}</div><div className="product-grid product-grid-editorial">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>})}{!hasCmsProducts&&latest.length>0&&<section className="section section-tight home-product-section"><div className="section-head"><div><span className="eyebrow dark">LATEST</span><h2>Latest Collection</h2><p className="section-subtitle">Live products from the Priyasa catalog.</p></div><a className="text-link" href="/new-arrivals">View All →</a></div><div className="product-grid product-grid-editorial">{latest.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}{!hasCmsProducts&&fallbackBest.length>0&&<section className="section section-tight home-product-section"><div className="section-head"><div><span className="eyebrow dark">BEST SELLING</span><h2>Best Sellers</h2></div><a className="text-link" href="/shop">View All →</a></div><div className="product-grid product-grid-editorial">{fallbackBest.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}{!hasCmsProducts&&festivalProducts.length>0&&<section className="section section-tight home-product-section festival-home-section"><div className="section-head"><div><span className="eyebrow dark">COLLECTION</span><h2>Festival Collection</h2></div><a className="text-link" href={`/category/${festival}`}>Shop Collection →</a></div><div className="product-grid product-grid-editorial">{festivalProducts.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}{!hasCmsProducts&&saleProducts.length>0&&<section className="section section-tight home-product-section"><div className="section-head"><div><span className="eyebrow dark">SALE</span><h2>Sale Favourites</h2></div><a className="text-link" href="/offers">View Offers →</a></div><div className="product-grid product-grid-editorial">{saleProducts.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}{!hasCmsCategoryGrid&&categories.length>0&&<section className="section section-tight home-categories"><div className="section-head"><div><span className="eyebrow dark">CATEGORIES</span><h2>Shop by Category</h2></div><a className="text-link" href="/shop">View All →</a></div><div className="category-grid category-grid-editorial">{categories.slice(0,6).map(c=><a className="category-card" key={c.id} href={`/category/${c.slug}`}><div className="category-image" style={{backgroundImage:`url(${c.imageUrl||''})`}}/><div className="category-label"><strong>{c.name}</strong><span>Shop Now →</span></div></a>)}</div></section>}{imageSlides.length>0&&<HomeImageCarousel slides={imageSlides}/>} {promos.map(p=><section className="split-banner" key={p.id}><div className="split-copy"><span className="eyebrow dark">{p.type.replaceAll('-',' ').toUpperCase()}</span><h2>{p.title||'The Priyasa edit'}</h2>{p.subtitle&&<p>{p.subtitle}</p>}{p.ctaHref&&<a className="button dark-button" href={p.ctaHref}>{p.ctaLabel||'Shop Now'} →</a>}</div><div className="split-image" style={{backgroundImage:`url(${p.imageUrl||''})`}}/></section>)}{features.length>0&&<section className="section section-tight"><div className="section-head"><div><span className="eyebrow dark">PRIYASA EDIT</span><h2>Style for Every You</h2></div></div><div className="feature-grid feature-grid-editorial">{features.map(f=><a key={f.id} href={f.ctaHref||'/shop'} className="feature" style={{backgroundImage:`linear-gradient(180deg,rgba(0,0,0,.05),rgba(0,0,0,.5)),url(${f.imageUrl||''})`}}><span className="eyebrow">{f.type.replaceAll('-',' ')}</span><h3>{f.title||'Explore the edit'}</h3><span>{f.ctaLabel||'Shop Now'} →</span></a>)}</div></section>}{customSections.map(s=><HomeCmsSection key={s.id} section={s}/>)}<AffiliateHomeSection/></>}
