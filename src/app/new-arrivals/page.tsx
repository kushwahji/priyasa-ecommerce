import {products} from '@/lib/catalog';import {ProductCard} from '@/components/ProductCard';export default function NewArrivals(){return <div className="page"><div className="breadcrumbs">Home / New In</div><h1 style={{font:'normal 38px Georgia,serif'}}>New Arrivals</h1><p className="muted">Fresh styles for every part of your wardrobe.</p><div className="product-grid shop">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></div>}
