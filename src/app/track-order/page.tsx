'use client';import {useState} from 'react';export default function Track(){const [id,setId]=useState('');const [message,setMessage]=useState('');return <div className="page"><div className="form" style={{margin:'50px auto',textAlign:'center'}}><div className="eyebrow">ORDER TRACKING</div><h1 style={{font:'normal 38px Georgia,serif'}}>Track Your Order</h1><p className="muted">Enter your Priyasa order number.</p><input className="input" placeholder="e.g. PRI-ABC123" value={id} onChange={e=>setId(e.target.value)}/><button className="button" onClick={()=>setMessage(id?`Tracking lookup prepared for ${id}. Connect your carrier API in Admin → Shipping.`:'Enter an order number.')}>Track Order</button>{message&&<p className="muted">{message}</p>}</div></div>}
