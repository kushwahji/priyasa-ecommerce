'use client';
import Link from 'next/link';import {useEffect,useMemo,useState} from 'react';import type {Product} from '@/lib/catalog';import {ProductCard} from '@/components/ProductCard';import {BagIcon,SearchIcon} from '@/components/StorefrontIcons';import {searchStorefront} from '@/lib/storefront-data';
const TRENDING=['Kurtis','Anarkali','Kurta Set','Cotton Kurti','Festive Wear'];const KEY='priyasa_recent_searches';
type F={category:string;brand:string;size:string;color:string;min:string;max:string;sort:string};const initial:F={category:'',brand:'',size:'',color:'',min:'',max:'',sort:'relevance'};
export default function AdvancedStorefrontSearch({initialQuery='',initialCategory=''}:{initialQuery?:string;initialCategory?:string}){
 const[q,setQ]=useState(initialQuery),[items,setItems]=useState<Product[]>([]),[facets,setFacets]=useState<any>({}),[meta,setMeta]=useState<any>({}),[f,setF]=useState<F>({...initial,category:initialCategory}),[recent,setRecent]=useState<string[]>([]),[focus,setFocus]=useState(false),[open,setOpen]=useState(false),[loading,setLoading]=useState(true),[suggestions,setSuggestions]=useState<any>({products:[],brands:[],categories:[]});
 useEffect(()=>{try{const raw=localStorage.getItem(KEY);const parsed=raw?JSON.parse(raw):[];setRecent(Array.isArray(parsed)?parsed.filter((x):x is string=>typeof x==='string').slice(0,6):[])}catch{setRecent([])}},[]);
 const save=(v:string)=>{const value=v.trim();if(!value)return;setRecent(a=>{const n=[value,...a.filter(x=>x.toLowerCase()!==value.toLowerCase())].slice(0,6);try{localStorage.setItem(KEY,JSON.stringify(n))}catch{}return n})};
 useEffect(()=>{let dead=false;const t=setTimeout(async()=>{setLoading(true);try{const params:Record<string,string>={q,per_page:'24'};Object.entries(f).forEach(([k,v])=>{if(v&&k!=='min'&&k!=='max')params[k]=v});if(f.min)params.min_price=f.min;if(f.max)params.max_price=f.max;const r=await searchStorefront(params);if(!dead){setItems(r.items.map((p:any)=>p));setFacets(r.facets||{});setMeta(r.meta||{})}}catch{if(!dead){setItems([]);setFacets({});setMeta({})}}finally{if(!dead)setLoading(false)}},180);return()=>{dead=true;clearTimeout(t)}},[q,f]);
 useEffect(()=>{let dead=false;const t=setTimeout(async()=>{if(q.trim().length<2){setSuggestions({products:[],brands:[],categories:[]});return}try{const r=await fetch('/api/v1/storefront/search/suggestions?q='+encodeURIComponent(q.trim()),{cache:'no-store'});const d=await r.json();if(!dead&&r.ok)setSuggestions(d.data||{products:[],brands:[],categories:[]})}catch{if(!dead)setSuggestions({products:[],brands:[],categories:[]})}},220);return()=>{dead=true;clearTimeout(t)}},[q]);
 const options=useMemo(()=>({categories:facets.categories||[],brands:facets.brands||[],sizes:facets.sizes||[],colors:facets.colors||[]}),[facets]);
 const count=Object.entries(f).filter(([k,v])=>k!=='sort'&&Boolean(v)).length;const choose=(v:string)=>{setQ(v);save(v);setFocus(false)};const set=(k:keyof F,v:string)=>setF(x=>({...x,[k]:v}));
 const facetSelect=(key:keyof F,label:string,values:any[])=> <div><b>{label}</b><select value={f[key]} onChange={e=>set(key,e.target.value)}><option value="">All {label.toLowerCase()}</option>{values.map((x:any)=><option key={x.value} value={x.value}>{x.label}{x.count!==undefined?` (${x.count})`:''}</option>)}</select></div>;
 return <div className="search-experience"><div className="search-mobile-head"><Link href="/" className="back" aria-label="Back">‹</Link><button type="button" className="mobile-search-box" onClick={()=>document.getElementById('priyasa-search-input')?.focus()}><SearchIcon/><span>{q||'Search products, styles & categories'}</span></button><Link href="/cart" className="bag-link" aria-label="Bag"><BagIcon/></Link></div><div className="search-hero"><span className="eyebrow dark">FIND YOUR PRIYASA EDIT</span><h1>What are you looking for?</h1><div className="search-input-wrap"><input id="priyasa-search-input" autoFocus value={q} onChange={e=>setQ(e.target.value)} onFocus={()=>setFocus(true)} onBlur={()=>setTimeout(()=>setFocus(false),180)} placeholder="Search kurtis, dresses, ethnic wear..." aria-label="Search products" autoComplete="off"/><button onMouseDown={e=>e.preventDefault()} onClick={()=>setQ('')} aria-label="Clear search">{q?'×':''}</button>{focus&&!q&&<div className="search-suggestion-panel"><b>Trending searches</b>{TRENDING.map(x=><button key={x} onMouseDown={()=>choose(x)}>⌕ {x}</button>)}{recent.length>0&&<><b>Recent searches</b>{recent.map(x=><button key={x} onMouseDown={()=>choose(x)}>↻ {x}</button>)}</>}</div>}{focus&&q&&((suggestions.products||[]).length>0||(suggestions.brands||[]).length>0||(suggestions.categories||[]).length>0)&&<div className="search-suggestion-panel live"><b>Suggestions</b>{(suggestions.products||[]).slice(0,5).map((p:any)=><Link key={'p'+p.id} href={`/product/${p.slug}`} onMouseDown={()=>save(q)}><span>{p.name}<small>{p.brand||p.category?.name||'Product'}</small></span></Link>)}{(suggestions.brands||[]).slice(0,3).map((b:any)=><button key={'b'+b.value} onMouseDown={()=>{set('brand',b.value);setFocus(false)}}>Brand · {b.label}</button>)}{(suggestions.categories||[]).slice(0,3).map((c:any)=><button key={'c'+c.value} onMouseDown={()=>{set('category',c.slug||'');setFocus(false)}}>Category · {c.label}</button>)}</div>}</div><div className="search-chips"><span>Try</span>{TRENDING.map(x=><button key={x} onClick={()=>choose(x)}>{x}</button>)}</div></div><div className="search-toolbar"><strong>{loading?'Finding…':`${Number(meta.total??items.length).toLocaleString('en-IN')} styles`}{q&&<span> for “{q}”</span>}</strong><div className="search-toolbar-actions"><button className="filter-toggle" onClick={()=>setOpen(x=>!x)}>Filters {count>0&&<b>{count}</b>}</button><label className="sort-control">Sort <select value={f.sort} onChange={e=>set('sort',e.target.value)}><option value="relevance">Relevance</option><option value="newest">Newest</option><option value="price_asc">Price: Low to High</option><option value="price_desc">Price: High to Low</option><option value="discount">Discount</option><option value="rating">Rating</option><option value="popular">Popular</option></select></label></div></div>{open&&<section className="search-filter-panel">{facetSelect('category','Category',options.categories)}{facetSelect('brand','Brand',options.brands)}{facetSelect('size','Size',options.sizes)}{facetSelect('color','Color',options.colors)}<div><b>Price range</b><div className="price-inputs"><input inputMode="numeric" placeholder="Min ₹" value={f.min} onChange={e=>set('min',e.target.value.replace(/\D/g,''))}/><input inputMode="numeric" placeholder="Max ₹" value={f.max} onChange={e=>set('max',e.target.value.replace(/\D/g,''))}/></div>{facets.price&&<small>Available: ₹{Number(facets.price.min||0).toLocaleString('en-IN')} – ₹{Number(facets.price.max||0).toLocaleString('en-IN')}</small>}</div><div className="filter-actions"><button className="clear-filter" onClick={()=>setF(initial)}>Clear all</button><button className="button" onClick={()=>setOpen(false)}>Apply</button></div></section>}<section className="section search-results-section">{items.length?<><div className="product-grid product-grid-editorial">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div>{Number(meta.last_page||1)>Number(meta.current_page||1)&&<div className="search-load-more"><button className="button" disabled={loading} onClick={async()=>{const next=Number(meta.current_page||1)+1;setLoading(true);try{const params:Record<string,string>={q,per_page:'24',page:String(next)};Object.entries(f).forEach(([k,v])=>{if(v&&k!=='min'&&k!=='max')params[k]=v});if(f.min)params.min_price=f.min;if(f.max)params.max_price=f.max;const r=await searchStorefront(params);setItems(a=>[...a,...r.items.map((p:any)=>p)]);setMeta(r.meta||{})}finally{setLoading(false)}}}>Load more</button></div>}</>:!loading&&<div className="search-empty"><h3>No exact match yet.</h3><p>Try a simpler spelling, category, color or size.</p><div className="zero-result-suggestions">{TRENDING.map(x=><button key={x} onClick={()=>choose(x)}>{x}</button>)}</div><button className="button" onClick={()=>{setQ('');setF(initial)}}>Explore latest launches</button></div>}</section></div>}
