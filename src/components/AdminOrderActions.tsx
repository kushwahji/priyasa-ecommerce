'use client';import {useState} from 'react';
export default function AdminOrderActions({orderId,status,hasShipment,paymentStatus,total}:{orderId:string;status:string;hasShipment:boolean;paymentStatus:string;total:number}){const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');async function call(url:string,opts:any){setBusy(true);setMsg('');try{const r=await fetch(url,opts);const d=await r.json();if(!r.ok)throw new Error(d.error||'Action failed');setMsg('Done');window.location.reload()}catch(e){setMsg(e instanceof Error?e.message:'Action failed')}finally{setBusy(false)}}return <div style={{display:'flex',gap:6,flexWrap:'wrap'}}><select className="input" defaultValue={status} disabled={busy} onChange={e=>call(`/api/admin/orders/${orderId}/status`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:e.target.value})})}><option>PAYMENT_PENDING</option><option>CONFIRMED</option><option>PROCESSING</option><option>SHIPPED</option><option>DELIVERED</option><option>CANCELLED</option><option>RETURN_REQUESTED</option><option>RETURNED</option><option>REFUNDED</option></select>{!hasShipment&&['CONFIRMED','PROCESSING'].includes(status)&&<button className="button" disabled={busy} onClick={()=>call(`/api/admin/orders/${orderId}/shipment`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})})}>Create Shipment</button>}{paymentStatus==='CAPTURED'&&status!=='REFUNDED'&&<button className="button secondary" disabled={busy} onClick={()=>{const v=prompt(`Refund amount (max ₹${total})`,String(total));if(v)call(`/api/admin/orders/${orderId}/refund`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({amount:Number(v)})})}}>Refund</button>}{msg&&<small className="muted">{msg}</small>}</div>}
