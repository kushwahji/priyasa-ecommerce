'use client';import {useState} from 'react';
export default function ReturnRequest({orderId,eligible,existing}:{orderId:string;eligible:boolean;existing?:{status:string}|null}){const [open,setOpen]=useState(false);const [reason,setReason]=useState('');const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');if(existing)return <div className="shipment-box"><strong>Return request</strong><p>{existing.status.replaceAll('_',' ')}</p></div>;if(!eligible)return null;async function submit(){if(reason.trim().length<5){setMsg('Please provide a short reason.');return}setBusy(true);setMsg('');try{const r=await fetch(`/api/customer/orders/${orderId}/return`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reason})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to request return');setMsg('Return request submitted.');setOpen(false);window.location.reload()}catch(e){setMsg(e instanceof Error?e.message:'Unable to request return')}finally{setBusy(false)}}return <div className="return-action"><button className="button" onClick={()=>setOpen(!open)}>{open?'Cancel':'Request return'}</button>{open&&<div className="admin-card" style={{marginTop:10}}><label>Reason<textarea className="input" rows={4} value={reason} onChange={e=>setReason(e.target.value)} placeholder="Tell us why you want to return this order."/></label><button className="button dark-button" disabled={busy} onClick={submit}>{busy?'Submitting…':'Submit return request'}</button></div>}{msg&&<small className="muted">{msg}</small>}</div>}
