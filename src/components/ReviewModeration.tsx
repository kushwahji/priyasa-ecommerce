'use client';import {useState} from 'react';export default function ReviewModeration({id,approved}:{id:string;approved:boolean}){const [busy,setBusy]=useState(false);async function set(v:boolean){setBusy(true);try{const r=await fetch(`/api/admin/reviews/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({approved:v})});if(!r.ok)throw new Error();location.reload()}catch{alert('Unable to update review')}finally{setBusy(false)}}return <div style={{display:'flex',gap:6}}>{!approved&&<button className="button" disabled={busy} onClick={()=>set(true)}>Approve</button>}{approved&&<button className="button secondary" disabled={busy} onClick={()=>set(false)}>Unpublish</button>}</div>}
