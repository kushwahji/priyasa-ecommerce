'use client';
import {useState} from 'react';
export function NewsletterForm(){const [email,setEmail]=useState('');const [state,setState]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setState('saving');try{const r=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Unable to subscribe');setState('Thanks! You’re subscribed.');setEmail('')}catch(err){setState(err instanceof Error?err.message:'Unable to subscribe')}}return <form className="newsletter-form" onSubmit={submit}><label htmlFor="newsletter-email">Email address</label><div><input id="newsletter-email" type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Enter your email"/><button className="button dark-button" disabled={state==='saving'}>{state==='saving'?'Joining…':'Subscribe'}</button></div>{state&&state!=='saving'&&<small>{state}</small>}</form>}
