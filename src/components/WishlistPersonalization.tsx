'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';
export default function WishlistPersonalization(){const [saved,setSaved]=useState(0),[recent,setRecent]=useState(0);useEffect(()=>{try{setRecent(JSON.parse(localStorage.getItem('priyasa_recently_viewed')||'[]').length)}catch{};fetch('/api/customer/wishlist',{cache:'no-store'}).then(r=>r.json()).then(d=>setSaved((d.data||[]).length)).catch(()=>{})},[]);return <section className="wishlist-personalization"><div><span className="eyebrow dark">YOUR STYLE, YOUR LIST</span><h3>Keep your favourites close</h3><p>{saved>0?`${saved} saved style${saved===1?'':'s'} ready when you are.`:'Save pieces you love for later.'} {recent>0&&`You have ${recent} recently viewed style${recent===1?'':'s'} to revisit.`}</p></div><div className="wishlist-personalization-actions"><Link href="/wishlist" className="button">View Wishlist</Link><Link href="/shop" className="text-link">Continue shopping →</Link></div></section>}
